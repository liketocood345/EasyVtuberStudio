Example character images for distillation live under each repo's `data/images/` when you add them locally.

EasyVtuberStudio ships the **bai** student under `data/character_models/baiten_from_project_forlon9/bai_450k/` instead of the upstream THA4 author examples (`lambda_00`, `lambda_01`), which are not included.
