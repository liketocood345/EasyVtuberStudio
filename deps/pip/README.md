# Load Preview pip 依赖（仓库内路径）

`venv` 通过 `resolve_venv.bat` 按**仓库根目录**解析，顺序：

1. `<repo>/venv`
2. `<repo>/talking-head-anime-4-demo/venv`
3. `<repo>/face-puppeteer-ui-enhancements-ai-code/talking-head-anime-4-demo/venv`

THA3 运行时资产见仓库根 `deps/tha3/`（非本目录）。
